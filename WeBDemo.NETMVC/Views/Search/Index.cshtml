
@{
    ViewBag.Title = "Index";
}

@foreach (var product in Model)
{
    <div>
        <h3>@product.Name</h3>
        <p>Giá: @product.Price</p>
    </div>
}

@if (Model.Count == 0)
{
    <p>Không tìm thấy sản phẩm phù hợp.</p>
}

